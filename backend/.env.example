# Backend environment variables (prototype)

# CORS
ALLOWED_ORIGINS=*

# Gemini (required for "real" AI). Pick ONE auth mode:
#
# Mode A (recommended on Render): API key (Google AI Studio)
# GEMINI_API_KEY=...
#
# Mode B: Vertex AI (service account JSON file)
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_LOCATION=us-central1
# GOOGLE_SERVICE_ACCOUNT_FILE=/etc/secrets/google-credentials.json

# Log level (debug/info/warn/error)
LOG_LEVEL=info

# In-memory session TTL (prototype). Note: Render may restart/scale instances, so sessions aren't durable.
SESSION_TTL_SECONDS=21600

# Client event logs (JSONL). On Render, this is ephemeral unless you attach a persistent disk.
EVENTS_DIR=/tmp/agent-social-events

# Admin password for /api/v1/admin/* endpoints.
# IMPORTANT: Frontend-only auth is not secure; keep admin APIs protected.
ADMIN_PASSWORD=jacksoncui@oriscen.ai

# Firebase Admin SDK (for Google ID token verification)
# Can be either:
#   - A file path: /etc/secrets/firebase-credentials.json
#   - Raw JSON string of the service account
FIREBASE_SERVICE_ACCOUNT_JSON=/etc/secrets/firebase-credentials.json
