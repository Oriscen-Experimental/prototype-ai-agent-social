# Backend environment variables (prototype)

# CORS
ALLOWED_ORIGINS=*

# Gemini (required for "real" AI). Pick ONE auth mode:
#
# Mode A (recommended on Render): API key (Google AI Studio)
# GEMINI_API_KEY=...
#
# Mode B: Vertex AI (service account JSON file)
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_LOCATION=us-central1
# GOOGLE_SERVICE_ACCOUNT_FILE=/etc/secrets/google-credentials.json

# Log level (debug/info/warn/error)
LOG_LEVEL=info

# In-memory session TTL (prototype). Note: Render may restart/scale instances, so sessions aren't durable.
SESSION_TTL_SECONDS=21600
