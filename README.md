# prototype-ai-agent-social

Frontend-only prototype (all mock): onboarding → search → 3 hard-coded journeys → matching list → profile reasons/badges → chat + calendar invite (mock).

## Run locally

```bash
cd prototype-web
npm install
npm run dev -- --host 0.0.0.0 --port 5173
```

## Deploy on Render

- Uses `render.yaml` (Static Site) with SPA rewrite.

