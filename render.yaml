services:
  - type: web
    name: agent-social
    runtime: docker
    rootDir: .
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: ALLOWED_ORIGINS
        value: "*"
      - key: SESSION_TTL_SECONDS
        value: "21600"
