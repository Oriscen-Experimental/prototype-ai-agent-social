services:
  - type: web
    name: agent-social-prototype
    env: static
    rootDir: prototype-web
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

